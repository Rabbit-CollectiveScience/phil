# Unit Tests

This directory contains fast unit tests that use mocking and don't require external services.

## Test Categories

- `l3_infra/` - Infrastructure layer tests (repositories, storage, config)
- `l2_domain/` - Domain layer tests (models, controllers) - future
- `l1_api/` - API layer tests (route handlers) - future